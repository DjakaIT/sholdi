/**
 * Placeholder. Exists so the tab bar has a real destination; the screen itself is
 * built later in the ARCHITECTURE.md §6 build order. Intentionally empty rather
 * than filled with invented content.
 */
import { View } from 'react-native';

import { colors } from '@/theme/tokens';

export default function StatsScreen() {
  return <View style={{ flex: 1, backgroundColor: colors.page }} />;
}
